export class PriceData {
    originalPrice: number;
    sellingPrice: number;

    constructor(originalPrice: number, sellingPrice: number) {
        this.originalPrice = originalPrice;
        this.sellingPrice = sellingPrice;
    }
}
