export class StoreData {
    id: string;
    name: string;
    city: string;
    country: string;

    constructor(id: string, name: string, city: string, country: string) {
        this.id = id;
        this.name = name;
        this.city = city;
        this.country = country;
    }
}
